# Habits

App de hábitos e planner gratuito, local-first (funciona offline, sem login), para iOS, Android e web.

Feito com Expo + React Native + TypeScript, Expo Router, expo-sqlite + Drizzle e Zustand.

## Rodando

```bash
npm install
npm run web      # navegador em http://localhost:8081
npm start        # QR code para o Expo Go (Android/iOS)
```

## Qualidade

```bash
npm run check    # lint + typecheck + testes
```

Convenções e arquitetura: veja [CLAUDE.md](CLAUDE.md).
